<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
    <id>{{ base_href }}{{ url_for('get_index') }}</id>
    <link href="{{ base_href }}{{ url_for('get_atom') }}" rel="self" type="application/atom+xml" />
    <link href="{{ base_href }}{{ url_for('get_index') }}" rel="alternate" />
    <title>Windowbox</title>
    <subtitle>Because moblogs aren't hip anymore.</subtitle>
    <updated>{{ items[0].created_utc.isoformat() }}</updated>

    {% for post in items %}
        {% set attachment = post.get_attachment() %}
        <entry>
            <id>{{ base_href }}{{ url_for('get_post', post_id=post.id) }}</id>
            <link href="{{ base_href }}{{ url_for('get_post', post_id=post.id) }}" rel="alternate" />
            <title>{{ post.message }}</title>
            <updated>{{ post.created_utc.isoformat() }}</updated>
            <summary type="html"><![CDATA[
                <p>
                    <a href="{{ base_href }}{{ url_for('get_post', post_id=post.id) }}">
                        <img src="{{ base_href }}{{ attachment.get_derivative_url(300, 300) }}" alt="{{ post.message }}" border="0" />
                    </a>
                </p>
            ]]></summary>
        </entry>
    {% endfor %}
</feed>
